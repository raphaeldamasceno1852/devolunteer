import styled from "styled-components";

export const Logo = styled.span`
	display: flex;
	h3 {
		display: flex;
		font-size: 2rem;
		font-family: "Inter";
		color: var(--rose-600);
		font-weight: 900;
		align-items: center;
	}

	h4 {
		display: flex;
		font-size: 2rem;
		font-family: "Inter";
		color: var(--blue-600);
		font-weight: 900;
		align-items: center;
	}
`;
